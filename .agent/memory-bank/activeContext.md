# Active Context

## Current Work
- **BRRTRouter PRD — hot-path v2 (2026-04-17):** Authored `BRRTRouter/docs/PRD_HOT_PATH_V2_STABILITY_AND_PERF.md` — phased plan to stop the "Hauliage microservice needs reboot" pattern. Phase 0 fixes per-response `Box::leak` (root cause of unbounded RSS), bounds metrics `DashMap` path keys, and retires the `may_minihttp` fork pin now that our upstream PR #21 (`MaxHeaders`) is merged (2026-01-05). Phase 1 `ArcSwap`, Phase 2 header-name intern + debug-gating, Phase 3 parker-based reply (no per-req channel), Phase 4 JWT de-dup, Phase 5 real bounded queue, Phase 6 Goose v2 scenario matrix + JSON baselines + soak job. Noted `may_minihttp` upstream `Response::header(&'static str)` lifetime still blocks Phase 0.1 — needs a second upstream PR. Updated BRRTRouter llmwiki `index.md` + `log.md`.
- **BRRTRouter rebase (2026-04-17):** `pre_BFF_work` in `microscaler/BRRTRouter` rebased onto latest `origin/main` (5 topic commits replayed; tip `6083a83`). Diverged from `origin/pre_BFF_work` — push with `--force-with-lease` when ready. Untracked: `docs/PRD-326-review-remediation.md`.
- **AGENT.md raw SQL rule (2026-04-17):** Core rule §5 in `AGENT.md` mirrors wiki policy; `docs/llmwiki/log.md` append. Committed (`farm git commit --no-add`).
- **Lifeguard llmwiki — raw SQL policy (2026-04-17):** Added `docs/llmwiki/topics/raw-sql-vs-selectquery-policy.md` (raw SQL last resort; human approval; ADR proving idiomatic ORM extension infeasible). Updated `query-select-and-active-model.md`, `workspace-and-module-map.md`, `index.md`, `docs-catalog.md`, `log.md`. Committed (`farm git commit --no-add`).
- **GraphQL docs alignment (2026-04-17, Lifeguard):** Recorded platform decision — GraphQL does **not** fit OpenAPI/BRRTRouter BFF; optional `graphql` / `async_graphql` is legacy/frozen. Updated `docs/llmwiki/topics/graphql-optional-feature.md` (top), `index.md`, `docs-catalog.md`, `log.md`; `README.md`, `src/lib.rs` rustdoc block, `Cargo.toml` comments, `LIFEGUARD_GAP_ANALYSIS.md` §6, `SECURITY_PROMPT.md`. Committed on lifeguard (`farm git commit --no-add`).
- **llmwiki initiative continuation (2026-04-17):** **Hauliage:** `reference/x-brrtrouter-impl-coverage.md` (152/144/8 from F5 D3.5), `topics/bff-view-composition.md` (WIP PRD synthesis), `topics/adr-quick-index.md`; `index` + `docs-catalog` + `log` updated. **Lifeguard:** `entities/transaction-boundaries.md`, `topics/graphql-optional-feature.md`; `docs-catalog` synthesis rows; `log` append. **BRRTRouter:** `topics/canonical-docs-vs-wip.md` (staleness policy for `docs/wip/`); index + docs-catalog + log. Commits: `farm git commit --no-add` per repo.
- **Lifeguard + BRRTRouter llmwiki comprehensive coverage (2026-04-17):** Lifeguard `docs/llmwiki/` now maps major subsystems: **reference** (`workspace-and-module-map`, `planning-docs-index`), **entities** (`life-model-and-life-record`, `life-executor-pool-and-routing`, `migrate-compare-and-sql-generation`), **topics** (query/active model, relations/scopes, session, reflector/cache, postgres scalars, observability, migrate CLI, derive, integration testing, prior index/BRR topics). `docs-catalog.md` includes a synthesis index table; `index.md` is a full catalog. **BRRTRouter** gained `topics/runtime-stack-map.md`, `generator-cli-and-askama.md`, `sibling-repos-and-wikis.md`; `index.md` + `docs-catalog.md` updated; `AGENTS.md` links **`../lifeguard/docs/llmwiki/`**. Commits: `farm git commit --no-add` on each repo.
- **Lifeguard llmwiki scaffold (2026-04-17):** Added Karpathy-pattern `docs/llmwiki/` (`SCHEMA.md`, `README.md`, `index.md`, `log.md`, `docs-catalog.md`) and seed topics `index-and-derive-constraints`, `brrtrouter-integration-pitfalls`, `documentation-landscape`. Rewrote root `AGENT.md` to strict rules + mandatory read of `docs/llmwiki/`; linked wiki from root `README.md`. Sibling-repo links use three `../` from `docs/llmwiki/` and four from `topics/` to reach `microscaler/` siblings (`hauliage`, `BRRTRouter`).
- **Hauliage llmwiki ingest (2026-04-17):** Ingested hauliage ADRs (through **0017**), postmortems, PRDs, and tooling pointers into `hauliage/docs/llmwiki/`: expanded `docs-catalog.md`; added wiki pages `topics/three-layer-defense.md`, `topics/scaffolding-lifecycle.md`, `entities/entity-vehicle.md`; updated `index.md`, `log.md`, and `docs/README.md` (sequential ADR table + cross-links). Planned wiki items for vehicle/scaffolding/three-layer defense are **done**; remaining planned: `bff-view-composition` topic, optional `x-brrtrouter-impl` reference page.
- **PRD_BFF_SCAFFOLDING_REMEDIATION.md v0.1 + hauliage safety tag (2026-04-17):** Carved the F5 audit + BRRTRouter Tier 1 work out of PRD_BFF_VIEW_COMPOSITION.md (v0.3) into its own actionable PRD, scoped narrowly so it can ship without blocking on the broader view-composition design discussion. **Sections:** §1 why (audit evidence from fleet/consignments/locations), §2 scope (in: remaining services audit + F5 patches + orphan disposition + BRRTRouter Fix A/B + regenerate + parity test; out: typed clients, views, Tier 2/3, openapi_bff consolidation), §3 deliverables D1–D7 (audit report, F5 patches, orphan tickets, Fix A template, Fix B library function, regenerated services, parity test), §4 match-arm fix pattern (mechanical), §5 acceptance criteria per deliverable, §6 phased plan A–E (~7–10 days total: audit → patches → Tier 1 → regenerate → close out), §7 risks (main.rs customization collision like fleet's `set_extra_prometheus`, intentional orphans, parity test false negatives, breaking BRRTRouter consumers), §8 flags a farm CLI gap (`farm git` has no `tag` subcommand — filed as minion-farm enhancement). **Safety checkpoint**: tagged `pre-scaffolding-remediation-2026-04-17` on hauliage HEAD (`bddce31`) **locally only, not pushed** per user rule requiring explicit push permission. Working-directory state preserved: fleet + consignments F5 patches from earlier + PRD v0.3 + this new PRD all uncommitted as expected. **Closure criterion**: every hauliage service's `impl/main.rs` ≤40 lines, `impl/registry.rs` generator-owned, zero hand-maintained match arms.
- **BRRTRouter multipart 415 fix + fleet 3-layer defense-in-depth + ADR 0016 (2026-04-17 third pass):** (1) **BRRTRouter multipart bypass closed**: added `pub request_content_types: Vec<String>` to `RouteMeta` populated from every key under `requestBody.content` (via new `extract_request_body_details`), wired into the spec builder. `server::service::call` gained a **V1a Content-Type enforcement** block that returns **HTTP 415** with `Accept-Post` header listing declared types when a request's Content-Type is not declared by the operation. Multipart fabrication of `Some(json!({}))` in `parse_request_body` removed — now returns `None` so the 415 gate handles it uniformly. 270/270 BRRTRouter lib tests pass; 6 test fixtures updated to include the new field. (2) **Fleet entity defense-in-depth**: new migration `migrations/fleet/20260417170000_vehicles_uniqueness_and_compliance.sql` adds partial unique indexes `vehicles_vin_unique` + `vehicles_registration_unique` (WHERE deleted_at IS NULL) with ADR 0015 idempotent retrofit pattern. **13 duplicate-VIN rows soft-deleted** during apply. (3) **Compliance-lifecycle**: `VehicleService::create` now hardcodes `OUT_OF_SERVICE` (was `ACTIVE`) because `CreateVehicleRequest` carries no MOT/insurance/tax dates. New `VehicleUpdateError` enum with `ActiveRequiresCompliance { missing_fields }` variant — `update_vehicle` now refuses to transition `status=ACTIVE` while any expiry date is null. Controller maps to **HTTP 409** `compliance_required_for_active` with `missing_fields` array. Also maps `vehicles_vin_unique` / `vehicles_registration_unique` DB violations to `HTTP 409` with typed error codes `vin_already_registered` / `registration_already_registered`. (4) **BDD expanded**: `fleet/impl/tests/features/vehicles.feature` gained 3 scenarios (new vehicle OUT_OF_SERVICE, ACTIVE rejected without dates, ACTIVE succeeds with dates). Existing `ABC-123` scenario hardened to use per-run unique suffixes (`ABC-123-<uuid8>` + `TEST-VIN-<capacity>-<uuid12>`) so the new unique indexes don't reject BDD re-runs. `test_lifeguard_update_bug.rs` similarly updated (was literally using `type="Type"`, violating the CHECK constraint). Fleet tests: **27 pass** (was 18). (5) **ADR 0016 written**: `docs/adr/0016-three-layer-defense-entity-invariants.md` formalizes the OpenAPI schema / impl validator / PostgreSQL constraint triad. Includes full worked example for Vehicle entity (9 invariants × 3 layers), invariant-to-layer mapping rule table, compliance rules (never remove a layer; OpenAPI is source of truth; constraint names are deterministic; state-transitions only at layer 2; BDD coverage required per invariant). References ADR 0015 idempotent retrofit pattern and the BRRTRouter multipart fix. Endpoint state: `/api/v1/fleet/vehicles` returns 23 vehicles (9 new creations from compliance-lifecycle BDD runs land as OUT_OF_SERVICE; 14 from prior state).
- **Fleet UI empty-table regression — root-caused + defensively fixed (2026-04-17 second pass):** User reported Fleet page at `/transport/fleet` showing 0 vehicle rows despite earlier fix. Diagnosis: the `create_vehicle_validator` patch I shipped (adding enum check) requires a fleet pod redeploy to take effect; meanwhile the running pod still accepted `type = 'Type'` through `POST /vehicles`. At 15:35 UTC a new bad row was inserted (likely by a test attempt) → `GET /vehicles` 500'd on response validation → Solid's `createResource` throws and `vehicles() || []` silently falls back to empty → UI renders no rows with no error shown. **Three-layer defensive fix shipped**: (1) **DB CHECK constraint** added directly to live DB as `ALTER TABLE hauliage.vehicles ADD CONSTRAINT vehicles_type_enum_check CHECK (type IN ('20'' Container', ...))` via the `postgres` superuser (table is owned by postgres, not hauliage — `hauliage` role returns `ERROR: must be owner of table vehicles` on ALTER). Verified: bad INSERT now rejected with `new row for relation "vehicles" violates check constraint "vehicles_type_enum_check"`. (2) **Migration file** `hauliage/migrations/fleet/20260417160000_vehicles_type_enum_constraint.sql` written following ADR 0015 idempotent retrofit pattern — defensive DELETE of drift rows then idempotent `IF NOT EXISTS` CHECK add; added to `migrations/apply_order.txt`. So the constraint survives fresh `hauliage-db-init` runs. (3) **Frontend UX improvement** in `Fleet.jsx` — added `<Show when={fleetData.error}>` block rendering a `data-testid="fleet-load-error"` banner with the error message + `curl` diagnostic hint, plus a `<Show when={!fleetData.error && !fleetData.loading && vehicles().length === 0}>` empty-state row. Previously the fetch throw was silently swallowed → invisible failure mode; now users see "Failed to load fleet vehicles: [msg]". **Endpoint final state**: 200 OK, 18 vehicles, types `{Box Truck, Flatbed Truck, Low Loader (Heavy), Refrigerated Container, Tautliner}`. Validator code change still needs fleet pod redeploy via hauliage tilt to be active — but the DB constraint provides the final-line-of-defense that works immediately without redeploy. Lints clean, `cargo check -p hauliage_fleet` compiles, fleet tests still 18/18. UX fix also addresses a latent bug class: any API 500 from the list endpoint used to render a blank table silently.
- **Fleet vehicle Type enum drift — fixed (2026-04-17):** Root-caused the `/api/v1/fleet/vehicles` response validation failure. DB query showed **3 rows with `type = 'Type'`** (literal placeholder) — 2 created during today's Playwright BDD run at 14:41/14:42 UTC, 1 older at 09:25. Triage: (1) **Validator was incomplete** — `create_vehicle_validator::validate` only checked `type` was non-empty, never cross-checked against the OpenAPI enum `["20' Container", "40' Container", "Tautliner", "Low Loader (Heavy)", "Flatbed Truck", "Refrigerated Container", "Tanker", "Box Truck", "Livestock Carrier"]`. So `POST /vehicles` with `type: "Type"` was accepted and persisted. (2) **Tests were inserting their own bad data** — the cycle was self-sustaining: tests ran → created bad vehicles → next test run failed on `list_vehicles` response validation → more bad data from attempts. **Fix shipped**: (a) added `ALLOWED_VEHICLE_TYPES: &[&str]` const to the validator module (single source of truth until BRRTRouter-gen emits the enum as a Rust type), (b) extended `validate()` to reject non-matching `type` with explicit error listing permitted values, (c) deleted the 3 bad rows via `DELETE FROM hauliage.vehicles WHERE type = 'Type'`. **Verified**: `/api/v1/fleet/vehicles` now returns **15 vehicles cleanly** (down from 18 that included the 3 bad rows), distinct types `{Tautliner, Flatbed Truck, Low Loader (Heavy), Refrigerated Container}`. Fleet compliance summary still green (15 vehicles, 3 compliant, 12 unknown). Fleet cargo tests: 18/18 still pass (validator change didn't break anything). Remaining frontend test failures (e.g. `007_fleet_compliance_ui.spec.ts`, `006_manage_fleet_vehicles.spec.ts`) are blocked by the **separate auth-cascade issue** (BFF DNS resolution failure; tests use `storageState: 'e2e/auth/storage-haulier.json'` which can't be generated because haulier.setup.ts fails) — not this Type-enum issue. That cascade is parent PRD §2.3 "the tell" territory. Validator fix needs a fleet pod redeploy for the defensive layer to be active, but the current pod reads from the cleaned DB so the immediate failure mode is resolved without redeploy.
- **Full test-suite verification post-F5 remediation (2026-04-17):** User asked for all BDD and spec tests to complete. **Cargo workspace compile check**: `cargo check --workspace` initially failed — identity had 3 compile errors (arms I added for `get_current_organization`, `list_organization_team`, `invite_team_member` referenced impl files that exist on disk but are NOT in `controllers/mod.rs` — pure dead files, new "Z2" class distinct from Z1). Fixed by removing those 3 zombie arms and replacing with an explanatory NOTE; workspace now compiles clean (exit 0). **Cargo tests per modified service**: fleet 18/18 ✓, consignments 17/17 ✓, company 10/10 ✓, analytics/notifications/identity/inbox/telemetry 0 tests each (no test suites defined, consistent with their prior scaffold-only state). **Lifeguard Tilt test-nextest**: 1014/1014 ✓ (unaffected). **Frontend Playwright BDD** (`npm run test:bdd`): **80 passed, 25 failed, 150 skipped** across 2.9 min. Failures concentrated in `post-a-job.feature` (20 failures across 4 scenarios × 5 browser projects, all timing out waiting for `[data-testid="route-card-0"]` which depends on telemetry+consignments) and `shipper_company_and_home_pin.feature` (5 failures). **Frontend Playwright E2E** (`npm run test:e2e`): **18 passed, 56 failed, 52 did not run**, cascade triggered by `e2e-auth-haulier.setup.ts` failing — pages load landing `/` instead of auth flow. **Root cause diagnosis** (via direct curl probes): (1) **`/api/v1/identity/auth/login` through BFF returns `{"error":"DNS error: failed to lookup address information: Name does not resolve"}`** — the BFF proxy controller template resolves `{service}.svc.cluster.local` which doesn't work from local BFF process. Pre-existing architectural issue exactly matching PRD_BFF_VIEW_COMPOSITION §2.3 "tell" diagnostic. (2) **`/api/v1/fleet/vehicles` returns response validation error `"Type" is not one of "20' Container", "40' Container" or 7 other candidates`** — impl returns data not matching OpenAPI enum, likely stale seed data or schema drift. (3) **`/api/v1/fleet/compliance-summary` works correctly** returning `{compliant_count:3, total_vehicles:18, unknown_count:15, ...}`. **Conclusion**: F5 remediation successfully flipped services from gen-stub mocks to real impls. The mocks had been spuriously passing tests by returning canned OpenAPI example data. Real impls now expose pre-existing bugs (DNS in BFF proxy, Type enum drift in vehicle schema) that were masked. This is the correct, expected signal from the F5 fix — **the scaffolding work worked; it surfaced impl bugs that need separate remediation**. Fleet compliance impl (which I shipped today) passes all its checks. Not a regression from F5 fixes. New Z2 class (files-on-disk-not-in-mod.rs) documented as a distinct category from Z1 (in-mod.rs-but-no-openapi-route); identity had 3 Z2 cases that compile-broke until fixed.
- **D3.5 `x-brrtrouter-impl` backfill complete across hauliage (2026-04-17):** Ran a Python-driven backfill across all 15 hauliage service openapi files + the merged `openapi_bff.yaml`. **Final state**: 152 total operations, 144 marked `x-brrtrouter-impl: true`, 8 marked `false`, 0 gaps. **16 openapi files modified** (111 insertions, 36 deletions in openapi_bff.yaml from the merged-spec propagation; +2–19 lines in each per-service file). **8 `false` markers** distributed across the 4 distinct orphan/scaffold ops: `consignments.log_job_exception` (orphan, no impl file), `storage.generate_document_download_url` (orphan, pair'd with upload which IS impl'd), `recommendations.get_job_matches` + `recommendations.trigger_matching_sweep` (pure skeleton service — has `gen/` but no `impl/` at all). Markers on service files + matching markers in `openapi_bff.yaml` = total 8. **New finding surfaced: Z1 (Zombie Impl)** — `identity/impl/src/controllers/` has 3 impl files whose operationIds don't exist in `openapi/identity/openapi.yaml` at all (`get_current_organization`, `list_organization_team`, `invite_team_member` — appear to be copy-pasted from `company` service during an abandoned migration). Match arms I added for these in identity's `main.rs` during F5 fixes are dead code (harmless but noise — they'll never fire because the operationIds aren't in `routes`). Parent PRD v0.4 §7.6 Fix A's tri-state check will flag these as codegen warnings once Tier 1 ships. Documented in `F5_AUDIT_2026-04-17.md` D3.5 section + new Z1 section. PRD_BFF_SCAFFOLDING_REMEDIATION.md v0.2 scope item 3 (backfill) is now complete.
- **PRDs rev'd to v0.4 (main) and v0.2 (remediation) after x-extension audit (2026-04-17):** Grounded both PRDs in BRRTRouter's documented spec-first workflow (ADR 0001 "Register & Overwrite" from `BUILDING_WITH_BRRTRouter.md`) and the existing `x-` extension landscape across both codebases. **Key discoveries:** (1) **`x-brrtrouter-impl: true` already exists** as a latent convention in hauliage OpenAPIs — `consignments/openapi.yaml` has it on exactly the 5 pre-F5-fix registered ops. Nobody consumes it yet; it's the perfect foundation for Fix A's tri-state validation. Dropped proposed `x-impl-status` extension as parallel invention. (2) **BFF merger already exists** as Python `brrtrouter_tooling.workspace.bff.generate_system.generate_system_bff_spec` (via `hauliage bff generate-system`) — auto-injects `x-service`, `x-service-base-path`, `x-brrtrouter-downstream-path` on merged ops. PRD §5.4 rewritten to **extend** this tool rather than build a parallel Rust `brrtrouter-gen bff-merge`. (3) **Farm is deprecated in hauliage** (`hauliage/tooling/README.md` line 78) — `hauliage` CLI is the replacement. Withdrew v0.1 remediation PRD's "farm git tag" enhancement proposal. (4) **Existing BRRTRouter x- extensions catalogued** (`x-handler-*`, `x-brrtrouter-body-size-bytes`, `x-brrtrouter-stack-size`, `x-sse`, `x-cors`, `x-brrtrouter-cors`, `x-ref-name`); none conflict with proposed `x-passthrough` / `x-view`. **PRD v0.4 changes**: §2.7 reframed from "developers knew it was wrong" to "ADR 0001 Step 4b lifecycle drift"; §2.9 added (hexagonal mapping — gen=adapters, impl=core, DB/clients=outbound); §2.10 added (spec-first lifecycle documented); §5.4 rewritten (extend Python tool); §7.6 Fix A gains tri-state `x-brrtrouter-impl` validation (marker=true ∧ no impl file ⇒ hard error; no marker ∧ impl file ⇒ warning); §10 resolved 10.r (use `x-brrtrouter-impl`), 10.s (existing Python merger), 10.t (extension inventory). **Remediation PRD v0.2 changes**: §1 reframed with ADR 0001 citation; §2 scope adds D3.5 backfill `x-brrtrouter-impl: true` on 70+ ops; §8 split (8.1 farm deprecation note, 8.2 existing Python merger note); orphan disposition now machine-readable via `x-brrtrouter-impl: false`. **Audit report (F5_AUDIT_2026-04-17.md)** got a reframing section acknowledging the v1 "neglect" framing was unfair. Numbers unchanged — 35 F5s patched, 1 orphan, 0 remaining.
- **F5 scaffolding audit — complete across all 15 hauliage services (2026-04-17):** Executed D1 + D2 of `PRD_BFF_SCAFFOLDING_REMEDIATION.md` v0.1. **35 F5 bugs found, 35 patched, 0 remaining.** **1 ORPHAN** documented (`consignments.log_job_exception` — no impl controller, deferred product decision). **Audit report**: `hauliage/docs/F5_AUDIT_2026-04-17.md` with full per-service table + patch detail + structural pattern analysis. **Per-service result**: fleet (8/8 registered, fixed), consignments (11/11, fixed), company (19/19, fixed — was registering 8 of them against `hauliage_company_gen::controllers::*` gen stubs instead of `crate::controllers::*` impl — subtle variant of F5, looked registered but served mock), analytics (3/3, fixed from zero), notifications (3/3, fixed from zero), identity (6/6, fixed from zero), inbox (4/4, fixed from zero), telemetry (3/3, fixed from zero); already clean: locations (2), customs (2), marketing (5), storage (1 — uses nicer impl-first pattern), bidding (5), reviews (3); bff has 0 impl controllers by design (proxy-only, reshaped in PRD_BFF_VIEW_COMPOSITION). **Three structural patterns** identified in the hauliage codebase: Pattern A (register-then-override: fleet, consignments), Pattern B (impl-first with fallback: storage, company — company's variant drifted to wrong imports), Pattern C (register_from_spec only, zero overrides: 5 services — literally shipped with `// TODO: Update to register impl controllers` comments in production). All three patterns converge under BRRTRouter Tier 1 Fix A+B. **Lints clean** on all 6 modified `main.rs` (plus fleet+consignments from earlier). Hauliage HEAD still `bddce31`, all F5 patches + PRD_BFF_SCAFFOLDING_REMEDIATION.md + F5_AUDIT_2026-04-17.md uncommitted per user rule; safety tag `pre-scaffolding-remediation-2026-04-17` in place. **Next PRD deliverables** D3–D7 (orphan tickets, BRRTRouter Tier 1 implementation, regenerate services, parity test) — sequential follow-on work tracked in `PRD_BFF_SCAFFOLDING_REMEDIATION.md` §3.
- **F5 scaffolding bug audit + fixes across fleet/consignments/locations (2026-04-17):** [v1 audit superseded by complete-15-service audit above.] Audit found **7 missing registration arms across 2 services**. **Fleet**: 8 controllers in `impl/`, 7 arms — missing `get_fleet_compliance_summary` (the original bug). **Consignments (severe)**: 11 impl controllers, 5 arms — **6 missing**: `attach_job_document`, `delete_draft_job`, `list_draft_jobs`, `list_job_documents`, `tidy_text_with_ai`, `update_job_phase`. **Locations**: clean (2 controllers, 2 arms). **Additional finding**: `consignments.log_job_exception` has openapi operationId but **no impl controller file at all** — left unregistered with a NOTE comment pending product decision; serves gen-stub `example_json` on every request. Each silent-mock fallback meant the UI could have been reading `example_json` backend data for those routes since the openapi stubs were generated. Fixes applied: 1 arm to `fleet/impl/src/main.rs` + 6 arms to `consignments/impl/src/main.rs`, all following the service's existing `spawn_typed_with_stack_size_and_name` pattern with 20480 stack size. Lints clean. This is the exact F5 bug class from PRD v0.3 §2.7 and the **concrete evidence supporting Tier 1 (§7.7) as a Phase 1 hard prerequisite** — hand-maintained match arms decay across 12 months of controller additions. Once BRRTRouter Tier 1's `impl_registry.rs.txt` template (PRD v0.3 §7.6 Fix A) ships, this bug class becomes structurally impossible. **TODO**: audit the remaining hauliage microservices (reviews, customs, marketing, company, bff, bidding, telemetry, identity, notifications, inbox) for the same drift before Phase 1 — likely more F5 bugs lurking.
- **WIP PRD: BFF View Composition + `x-passthrough` — v0.3 (2026-04-17):** Enriched with scaffolding-maturity analysis after reviewing BRRTRouter's actual Askama template surface (eight templates: `Cargo.toml.txt`, `lib.rs.txt`, `main.rs.txt`, `handler.rs.txt`, `handler_types.rs.txt`, `controller.rs.txt`, `registry.rs.txt`, `mod.rs.txt` + impl variants) and hauliage's consumption of it. **Live bug surfaced:** fleet `impl/src/main.rs` override match has 7 arms but `impl/src/controllers/` has 9 controller files including the newly-shipped `get_fleet_compliance_summary.rs` — missing arm means `GET /api/v1/fleet/compliance-summary` silently serves gen-stub `example_json` mock data instead of real implementation. **Added:** §2.6 BRRTRouter template surface inventory, §2.7 registration seam with F1–F7 failure-mode taxonomy + fleet bug as worked example, §2.8 scaffolding regen problem (cookiecutter+cruft / partial classes / tonic prior art). **Rewrote §5.5:** typed clients now live as cargo-feature-gated module `client` inside existing `<service>/gen/` crate (not separate `client/` crate) — eliminates type duplication, splits `gen/Cargo.toml` deps into `server`/`client` features, BFF depends `default-features = false, features = ["client"]`. **Added §7.6** Fix A (`impl_registry.rs.txt` template — generator-owned registration, auto-discovered from controllers on disk), Fix B (`brrtrouter::server::run_app()` library helper — collapses 600-line duplicated security/CORS/dispatcher/HTTP-server block from every service's `impl/main.rs` into ~30-line delegator), Fix C (trait-per-operationId compile-time-enforced registration — optional). **Added §7.7** three-tier maturity ladder: Tier 1 strict file-boundary ownership (ships Fix A + B), Tier 2 manifest + 3-way merge (`.brrtrouter/manifest.json` + `imara-diff`), Tier 3 region markers (parked). **Added §7.8** concrete implementation plan — Askama renders THEIRS, `imara-diff` does 3-way merge with BASE snapshot + OURS on disk, new CLI `brrtrouter-gen plan|regen|status`. **Updated §8 Phase 0** — fleet bug fix + cross-service audit for F5-class bugs now hard exit criteria. **Phase 1** — Tier 1 now hard prereq. **Phase 3** — Tier 2 hard prereq. **Phase 4** — clients are feature-gated modules. **Resolved §10.e** may::join! chosen. **New §10.o/p/q** Fix A/B/C sequencing, Tier 2 timing, imara-diff vs. git merge-file choice. **New §9.9** risk around mid-flight scaffolding regen with additive-first service-by-service rollout. **Existing v0.2 note retained below.** **WIP PRD: BFF View Composition + `x-passthrough` — v0.2 (2026-04-17):** Revised to front-load reader education per feedback that the original v0.1 jumped straight to "what we propose" without the history + reasoning. New **§2 Background and context (required reading)** with five sub-sections: 2.1 timeline of how the proxy-only BFF came to exist; 2.2 what a BFF actually is (Sam Newman definition, five defining capabilities, and what a BFF is **not** — gateway, security layer, shared backend); 2.3 the "tell" diagnostic ("can you replace your BFF with an NGINX config without loss?" — currently yes, which is the signal); 2.4 explicit "why not GraphQL?" decision trace (what's shared, what GraphQL adds+charges for, what we lose, break-even analysis at ~10 dashboards, exit path to GraphQL preserved); 2.5 reframe that proxy-first was phase-appropriate, not naive. Also expanded §9 Risks from a 7-row table into 8 narrative sub-sections (§9.1–§9.8) each with named "shape of the risk" + mitigation pairs covering: god endpoints, UI/BFF release coupling, partial-failure blindness, cache invalidation (deferred), generator regen confusion, `x-passthrough` sprawl, merged spec drift, cargo graph explosion. Renumbered §2→§3 through §13→§14 and OPEN questions §9.x→§10.x throughout, including all cross-references. Changelog entry added. Same 14 OPEN questions; same non-goals; same migration plan. **WIP PRD: BFF View Composition + `x-passthrough` (2026-04-17):** Drafted `hauliage/docs/PRD_BFF_VIEW_COMPOSITION.md` v0.1 capturing the proposed evolution from proxy-only BFF to a two-layer design (passthrough layer annotated via `x-passthrough` on each microservice OpenAPI + view composition layer in a new `openapi/openapi_bff_views.yaml`). Design anchors: (1) BFF generator `brrtrouter-gen bff-merge` that combines `x-passthrough`-flagged operations from all microservice specs with views from the views file to emit `openapi_bff.generated.yaml` + passthrough controllers (full regen) + view controller stubs (first-write only); (2) typed downstream client crates via a new `brrtrouter-gen client` mode; (3) shared `SliceError` schema + `slice_errors[]` array on every view response for first-class partial failure; (4) `transport/revenue` pilot targeted (6 XHRs → 1, Vite `/api` proxy collapses to single rule). **14 explicit [OPEN] questions** (§9.a–§9.n) kept unresolved for review: scope of `x-passthrough`, `x-view.fan_out[]` grammar, commit-vs-gitignore generated spec, view regen strategy, concurrency primitive (`may::join!` vs BRRTRouter-native), HTTP status on partial failure, auth scoping location, caching, testing strategy (backend BDD + Playwright), URL shape `/api/v1/views/<area>/<name>`, versioning signal via typed client compile-break, rename policy (preference: no), observability auto-spans, ownership (frontend team owns views file). Converts to ADR 0016 when §9 is cleared + Revenue pilot meets §5.3 acceptance criteria. Lists phased migration (pilot → annotation sweep → merger → typed clients → view fleet → retire hand-edited `openapi_bff.yaml`).
- **Hauliage fleet — compliance rollout closure (2026-04-17):** OpenAPI `GET /compliance-summary` + BFF `GET /fleet/compliance-summary`, `get_fleet_compliance_summary` controller, backend BDD (`tests/features/fleet_compliance.feature` + `tests/bdd/fleet_compliance.rs`), frontend mock (`frontend/src/mocks/fleetComplianceUiMock.js`, default mock on) and Fleet / View / Edit pages wired. **New this step:** Playwright spec `frontend/e2e/specs/transport/007_fleet_compliance_ui.spec.ts` + Gherkin `frontend/e2e/features/fleet-compliance-ui.feature`; `empty_summary()` no longer incorrectly marked `dead_code`. **Verify on a machine with `hauliage`:** `cargo test -p hauliage_fleet`, Playwright with `e2e/auth/storage-haulier.json`; then set `USE_FLEET_COMPLIANCE_UI_MOCK=false` when BFF serves `/api/v1/fleet/compliance-summary` end-to-end.
- **Hauliage fleet — vehicle compliance (2026-04-17):** Domain logic in `hauliage/microservices/fleet/impl/src/services/vehicle_compliance.rs` evaluates MOT / insurance / tax dates (30-day warning window) → `COMPLIANT` | `WARNING` | `NON_COMPLIANT` | `UNKNOWN`; `compliance_api.rs` maps to generated `VehicleComplianceIssue`. OpenAPI: `openapi/fleet/openapi.yaml` + `openapi/openapi_bff.yaml` — new `VehicleComplianceIssue` / `FleetVehicleComplianceIssue`, `compliance_status` + `compliance_issues` on `Vehicle` / `FleetVehicle`, `tax_expiry_date` on update requests. Regenerated `fleet/gen` and `bff/gen` via `brrtrouter-gen`. Wired `list_vehicles`, `get_vehicle`, `create_vehicle`; removed placeholder expiry defaults (empty string when unset). `vehicle_service::update_vehicle` parses optional `tax_expiry_date` and uses `Option<String>` for date fields. Verified: `cargo test -p hauliage_fleet` (unit + bdd pass; `manual` test needs local DB).
- **Hauliage fleet — compliance seed data (2026-04-17):** `microservices/fleet/impl/seeds/20260409130001_seed_vehicles.sql` now drives **four** demo vehicles: (1) `CURRENT_DATE`-relative MOT/insurance/tax so roll-up stays **COMPLIANT**; (2) MOT `CURRENT_DATE + 14` for **WARNING**; (3) MOT `CURRENT_DATE - 10` for **NON_COMPLIANT**; (4) new UUID `b1000004-…` with all three expiry columns **NULL** for **UNKNOWN**. `20260411023500_seed_vehicles_enticements.sql` sets `base_rate` / `fuel` for the fourth row.
- **docs/ reorganization + llmwiki seeding (2026-04-17):** Restructured `hauliage/docs/` from a flat 36-file folder into 4 topic subdirs: `adr/` (16 files), `postmortems/` (5 files — added `postmortem_geographic_dependencies.md` and my new default-expr one), `discoveries/` (3 files — `DATABASE_LAYOUT.md`, `design-lifeguard-stored-procedures.md`, `BFF_GAP_ANALYSIS_AND_DESIGN.md`), and `llmwiki/` (new, seeded per [karpathy llm-wiki pattern](https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f) with `README.md` + `topics/{lifeguard-schema-authoring,seed-pipeline,idempotent-migrations}.md` + `entities/{entity-default-expr,entity-apply-order-txt,entity-seed-order-txt,entity-infer-zero-default}.md`). Root keeps PRDs (`PRD_*`, `prd-*`), `ROADMAP.md`, `BFF_TASKS.md`, and a new `docs/README.md` topic-grouped index. **Cross-reference sweep**: rewrote every relative link across moved files — sibling ADR refs stayed relative, postmortem refs became `../postmortems/…`, PRD refs became `../PRD_…`, `AGENTS.md` refs became `../../AGENTS.md`, and all `../microservices/` / `../openapi/` / `../../shared-kind-cluster/` paths in ADR 0009 bumped up one level. **External touchpoints updated**: `hauliage/AGENTS.md` (path rewrites + new top-of-file "Documentation layout" table + greatly-expanded "Lifeguard entities, indexes, foreign keys, and migrations" section now explicitly cross-linking ADRs 0013/0014/0015 and the relevant postmortems in the Defaults / Retrofit / Seeds subsections), `hauliage/scripts/setup-db.sh` (comment path), `hauliage/microservices/company/impl/seeds/README.md` (ADR 0015 link + postmortem path), `hauliage/Tiltfile`, `hauliage/.agent/memory-bank/techContext.md`, `hauliage/microservices/reviews/impl/src/controllers/get_review_stats.rs` (comment). **Verification**: Python-driven link-checker walks every markdown file under `docs/` + the known external touchpoints; all proper `[label](url)` links resolve. The only 4 "broken" links found are pre-existing in `prd-haulier-profile-compliance-routing.md` pointing at a never-existing `vehicle_compliance.feature` — unrelated to this reorg. **Naming convention for llmwiki pages**: `topics/` = cross-cutting themes (compound), `entities/` = long-lived first-class concepts (reference-style), both flat with no further nesting; cross-links use relative paths so the graph works in any markdown viewer.
- **ADRs 0013 / 0014 / 0015 — formalizing today's decisions (2026-04-17):** Authored three Architecture Decision Records in `hauliage/docs/` to codify the policies that emerged from today's `hauliage-db-init` iterations. **ADR 0013 — Seed File Ordering Contract** (`YYYYMMDDHHMMSS_<slug>.sql` grammar + `microservices/seed_order.txt` as single source of truth + `setup-db.sh` reads it preferentially with alphabetical `find | sort` as deprecated back-compat fallback); explicitly extends ADR 0011 which established *where* seeds live but said nothing about ordering; includes mermaid flow of the generator ↔ setup-db.sh handoff + behavior matrix for the enforcement (code-gen filter + stderr warning, skip still non-negotiable even under `LIFEGUARD_SILENCE_UNTIMESTAMPED_SEEDS=1`). **ADR 0014 — `#[default_expr]` Policy for NOT NULL Lifeguard Fields** (Category A / B / C three-bucket authoring model; auto-infer safety net spec; warning-as-tech-debt-signal; interaction with `generated_migration_diff`'s "columns already in baseline are skipped" semantics that makes retrofits necessary for policy changes); references ADR 0010 (timestamp alignment) and forward-references 0015. **ADR 0015 — Idempotent Schema Retrofit Pattern** (the 4-statement canonical `ADD COLUMN IF NOT EXISTS` → `SET DEFAULT` → `UPDATE WHERE IS NULL` → `SET NOT NULL` idiom; behavior matrix for all 4 initial-DB states converging on one final shape; worked example using the `verification_reminders_sent` retrofit; compliance rules including "never simplify back to a single `ADD COLUMN IF NOT EXISTS` — those 3 extra statements look redundant on fresh DBs and are load-bearing on stale ones"). All three cross-reference today's postmortem (`postmortem-lifeguard-default-expr-2026-04.md`) and each other; each carries the house-style `Date / Status / Scope / Context / Decision / Consequences / Compliance / References` structure matching ADRs 0009–0012. No code changes; pure documentation. Memory-bank entries for the three earlier today fixes remain intact — the ADRs are the "written-down rule" layer on top of the tactical + strategic fixes.
- **Seed filename enforcement + `industry`→`gics_industry_code` drift (2026-04-17):** Fixes the 3rd iteration of the `setup-db.sh` failure chain: `ERROR: column "industry" of relation "organization_profiles" does not exist` in `company_demo_organization.sql`. Two intertwined fixes. **(1) Tactical**: `git mv microservices/company/impl/seeds/company_demo_organization.sql 20260422000000_company_demo_organization.sql`; swapped the legacy `industry = 'Freight & Logistics'` for `gics_industry_code = '20301010'` (GICS Air Freight & Logistics sub-industry, resolvable via `seed_gics.sql` which runs at `20260421000000`, i.e. before the renamed demo seed). No other untimestamped seeds found across `microservices/*/impl/seeds/*.sql`. **(2) Strategic enforcement** in `lifeguard-migrate::sql_dependency_order::write_seed_order_file`: iterates `seed_files`, and for each filename calls `parse_leading_timestamp` — if it returns `None` (no strict `YYYYMMDDHHMMSS_<slug>.sql` prefix), the seed is **skipped** from `seed_order.txt` and a stderr warning is emitted naming the file + suggesting `git mv <name> {timestamp}_<name>` + pointing at the `LIFEGUARD_SILENCE_UNTIMESTAMPED_SEEDS=1` opt-out. Two pre-existing tests that had used untimestamped names (`a.sql` / `b.sql` / `company_demo_organization.sql` / `seed_normalized_locations.sql`) were updated to use valid timestamped names; a new test `seed_order_skips_seeds_without_timestamp_prefix` covers the skip + warning path directly (validates skipping `company_demo_orphan.sql` and `2026050_bad.sql` while keeping `20260501000000_seed_a.sql`). **Docs** updated: `lifeguard/lifeguard-migrate/README.md` new "Apply order and seed order" section documenting the strict convention + both env-var escape hatches; `hauliage/microservices/company/impl/seeds/README.md` rewritten to document the timestamp rule, the idempotent retrofit pattern, and the "entity drop/rename → update seeds in same PR" discipline; `hauliage/scripts/setup-db.sh::apply_seeds_from_disk` header note; `hauliage/docs/postmortem-lifeguard-default-expr-2026-04.md` new "Related enforcement: seed filename convention" section cross-linking the enforcement and the `industry`→`gics_industry_code` drift. Verified: `cargo test -p lifeguard-migrate --lib --tests` **170 pass / 0 fail**; `cargo clippy -p lifeguard-migrate --lib` 0 warnings; `cargo run -p hauliage_migrator` emits the renamed seed on line 12 of `seed_order.txt`, correctly placed after the GICS seed (line 11) and the locations seed (line 9).
- **Category A `#[default_expr]` adoption + stricter auto-infer warnings (2026-04-17):** Executed the postmortem follow-ups. Added explicit `#[default_expr]` to 8 hauliage LifeModel fields (`verification_reminders_sent: i16` → `"0"` already; `consent_marketing: bool` → `"false"`; `max_uses: i32` + `use_count: i32` on `MarketingInvite` → `"0"`; `active: bool` on `MarketingCampaign` → `"true"`; `quotes_submitted: i32` + `company_reviews_count: i32` on `Consignment` → `"0"`; `is_completed: bool` on `TelemetryTimeline` → `"false"`; `postcode_enabled: bool` on `LocationCountry` → `"true"` matching hand-written migration). Skipped `ReviewSummary.*` on inspection — it's a `#[view(query = …)]` so `sql_generator` early-exits before the default branch runs; adding `#[default_expr]` on view fields is a no-op. **`lifeguard-migrate::sql_generator::warn_auto_inferred_default`**: new private helper that fires `eprintln!("warning: [lifeguard-migrate] auto-inferred DEFAULT <v> for <type> NOT NULL column `<table>.<col>` (add #[default_expr = …] to the LifeModel field to silence; set LIFEGUARD_SILENCE_INFERRED_DEFAULTS=1 to silence all)")` every time the zero-default safety net kicks in; suggested-attr formatting adapts to `false` vs numeric defaults. Env var opt-out for CI / quiet test runs. Verified: `cargo test -p lifeguard-migrate --lib --tests` **169 pass / 0 fail**; `cargo clippy -p lifeguard-migrate --lib` 0 warnings; `cargo build` of all updated hauliage entity crates clean; `cargo run -p hauliage_migrator` reports every entity `Skipped` and now emits exactly 9 warnings — one per Category B field enumerated in `postmortem-lifeguard-default-expr-2026-04.md` (`payload_weight`, `pay_amount`, `manufacture_year`, `vehicle_classes.available`, `active_geofence_radius_meters`, `telemetry_locations.heading`, `quotes.amount`, `reviews.rating`, `customs_hs_codes.tree_level`). These warnings are the prompt for the next hygiene pass.
- **Postmortem: `#[default_expr]` and the NOT NULL column trap (2026-04-17):** Written `hauliage/docs/postmortem-lifeguard-default-expr-2026-04.md` capturing the three-failure chain (silent entity → generator emits NOT NULL without DEFAULT → `ADD COLUMN IF NOT EXISTS` shadows hand-written retrofit) + when `#[default_expr]` must/must-not be used + implications for `sql_generator` auto-infer + the `generated_migration_diff` "baseline doesn't re-emit already-present columns" trap + self-healing ALTER COLUMN retrofit pattern + recovery playbook + full audit table of hauliage entities currently relying on auto-infer vs those that must stay default-less. Matches existing `postmortem-*-2026-04.md` style and cross-links ADR 0004 (Option poisoning) and `postmortem-lifeguard-migration-sync-failures-2026-04.md`.
- **Self-healing retrofit for pre-DEFAULT marketing column (2026-04-17):** Second report of the same `verification_reminders_sent` NOT NULL violation after the generator fix. Reason: `ADD COLUMN IF NOT EXISTS` is a **full no-op** on DBs where the column predates the `DEFAULT 0` clause, so the prior file-level edit only helps fresh installs. Tactical retrofit landed in `hauliage/migrations/marketing/20260416120000_marketing_form_submission_reminders.sql`: (1) `ADD COLUMN IF NOT EXISTS … DEFAULT 0`, (2) **`ALTER COLUMN … SET DEFAULT 0`** (runs unconditionally — heals stale schema), (3) defensive `UPDATE … WHERE … IS NULL` backfill, (4) idempotent `SET NOT NULL`, (5) preserved `COMMENT ON COLUMN`. Header comment documents the shape so it doesn't get "simplified" back. Audited rest of `hauliage/migrations/`: only `locations/20260416104809_locations_countries.sql` has a similar `ADD COLUMN BOOLEAN NOT NULL` shape, but the hand-written `20260416101500_add_postcode_enabled.sql` runs first with `DEFAULT true`, so both fresh + stale DBs land OK. **Parked follow-up:** emit self-healing retrofit pattern from lifeguard codegen itself (trigger on the 3rd occurrence of this bug class). Verified: `cargo test -p lifeguard-migrate --lib --tests` still 169 pass / 0 fail.
- **Auto-inferred zero DEFAULT for NOT NULL numeric / boolean columns (2026-04-17):** Fixes `null value in column "verification_reminders_sent" violates not-null constraint` reported from `setup-db.sh` applying `marketing_dev_seed.sql`. Cause: Lifeguard emitted `ADD COLUMN IF NOT EXISTS verification_reminders_sent SMALLINT NOT NULL` (no DEFAULT) because the entity field `pub verification_reminders_sent: i16` had no `#[default_expr]`; a later hand-written `ADD COLUMN IF NOT EXISTS … DEFAULT 0` migration was silently shadowed by `IF NOT EXISTS`. **`lifeguard-migrate::sql_generator`:** new `infer_zero_default_for_sql_type(col_type)` → `Some("0")` for SMALLINT / INT / INTEGER / INT2/4/8 / BIGINT / NUMERIC / DECIMAL / REAL / FLOAT/4/8 / DOUBLE PRECISION / MONEY, `Some("false")` for BOOLEAN / BOOL, `None` for TEXT / VARCHAR / CHAR / UUID / JSON / JSONB / timestamps / DATE / TIME / BYTEA (those require explicit defaults — silent auto-default would mask real bugs). Wired in as the **fourth** DEFAULT branch: explicit `default_expr` > explicit `default_value` > UUID PK `gen_random_uuid()` > inferred zero default, gated on `!nullable && foreign_key.is_none() && !is_primary_key`. 4 new unit tests. **Tactical:** edited `hauliage/migrations/marketing/20260416070238_marketing_form_submission.sql` to add `DEFAULT 0` (diff engine can't retro-fix existing files because the column already lives in the baseline). The redundant hand-written `20260416120000_marketing_form_submission_reminders.sql` is now a no-op, left in place for audit continuity. Verified: `cargo test -p lifeguard-migrate --lib --tests` **169 pass / 0 fail**; `cargo run -p hauliage_migrator` reports every entity `Skipped`, `apply_order.txt` + `seed_order.txt` re-emitted cleanly.
- **Seed-order (FK-aware) generation (2026-04-17):** Fixes reported `setup-db.sh` failure `insert or update on table "registered_addresses" violates foreign key constraint … Key (country_code)=(ZW) is not present in table "locations_countries"`. Cause: alphabetical `find | sort` put the `company_demo_organization.sql` seed before `seed_normalized_locations.sql`. Same class as the apply-order bug, now for seed data. **`lifeguard-migrate::sql_dependency_order`**: new `extract_inserted_tables_from_sql` (INSERT INTO / COPY FROM / UPDATE … SET); `extract_table_level_fk_edges_from_migration_sql` (inline `CREATE TABLE (...)` body via balanced-paren scan + `ALTER TABLE X …;` via `;` at depth 0); `write_seed_order_file(migrations_dir, seeds_root, seed_files, out_path)` topologically orders seeds with the same timestamp → path → untimestamped tie-break used for apply-order, returns `Err` on genuine cycles, tolerates a missing migrations graph by falling back to deterministic timestamp/path order. 12 new tests. **Consumer (`hauliage`):** `microservices/migrator/src/main.rs` gains `discover_seed_files()` + a call to `write_seed_order_file` after `write_apply_order_file` (writes `microservices/seed_order.txt`). `scripts/setup-db.sh::apply_seeds_from_disk` reads that file when present (skipping `#` comments and blank lines, warning on missing entries) and falls back to the legacy `find | sort` alphabetical walk otherwise. Verified: `cargo test -p lifeguard-migrate --lib --tests` **165 pass / 0 fail**; `cargo clippy -p lifeguard-migrate --lib` 0 warnings; `cargo build -p hauliage_migrator` clean. **Operational:** next `cargo run -p hauliage_migrator` emits `seed_order.txt`; `setup-db.sh` (or the `hauliage-db-init` Tilt resource) picks it up automatically. Expected: `locations/…/seed_normalized_locations.sql` precedes `company/…/company_demo_organization.sql`.
- **View-aware migration ordering + per-table identity (2026-04-17):** Fixes reported consumer failure `relation "hauliage.company_gics_categories" does not exist` when applying a `CREATE OR REPLACE VIEW` file, and the "duplicate migration on every run" symptom. **`lifeguard-migrate::sql_dependency_order`**: `extract_created_tables_from_migration_sql` now also matches `CREATE [OR REPLACE] VIEW [IF NOT EXISTS]`; new `extract_view_source_tables_from_migration_sql` parses `FROM` / `JOIN` (incl. `INNER|LEFT|RIGHT|FULL [OUTER]`, `ONLY`, `LATERAL`, aliases, `"quoted"."schema".name`) from view bodies while excluding CTEs declared via `WITH`; `write_apply_order_file` unions those source tables into `refs` so a view can never be scheduled before its backing table. **New module `lifeguard_migrate::migration_writer`** (per-table file layout): `write_per_table_migration_file(service_dir, table_name, fresh_sql, timestamp, header)` with `EmissionOutcome::{Initial, Delta, Skipped}`, reusing `generated_migration_diff` for merged-baseline ALTER/INDEX deltas and `normalize_table_sql_blob` fast-skip that no longer breaks when a table has ≥ 2 historical files; views take the `CREATE OR REPLACE`-is-idempotent fast path. **Consumer `hauliage/microservices/migrator`**: deleted inline `write_migrations` / `collect_known_columns` / `collect_known_modifier_lines`; delegates to `write_per_table_migration_file`; single `run_timestamp` captured once in `main()`; `regex` dep removed. **Tests:** 20 in `sql_dependency_order` (was 7) + 17 in `migration_writer::tests` (was 0). Verified: `cargo test -p lifeguard-migrate --lib --tests` **153 pass / 0 fail**; `cargo clippy -p lifeguard-migrate --lib` 0 warnings; `cargo build -p hauliage_migrator` clean. **Follow-ups landed (same session):** (a) `parse_leading_timestamp` + `compare_apply_order_entries` — Kahn's `in_degree == 0` cohort is now tie-broken by parsed `YYYYMMDDHHMMSS` first (then path); FK/view/ALTER edges still dominate. (b) `parse_per_table_migration_filename` + stricter `accumulate_per_table_baselines_from_dir` — only `YYYYMMDDHHMMSS_<slug>.sql` files are considered; `_generated_from_entities.sql` still delegated to big-file accumulator.
- **ORM / derive — structured `IndexKeyPart` + infer-schema indexes (2026-03-28):** **`IndexKeyPart`** (`Column` / `Expression`) with opclass, COLLATE, ASC/DESC, NULLS; **`key_parts`** on **`IndexDefinition`** (legacy **`key_list_sql`** when empty); **`format_index_key_list_sql`** vs **`format_index_key_list_derive_value`** (derive uses `expr | coverage`). Derive: comma split at paren depth 0, structured column segments, `expr | cols`. **`infer-schema`:** `fetch_btree_indexes` + `#[index]` emission (skips expression indexes with empty inferred coverage); `emit_inferred_rust(..., indexes_by_table)`. **Docs:** PRD §5.7, `SEAORM_LIFEGUARD_MAPPING.md`, `DESIGN_INDEX_COMPARE_T2B_T3.md` §4.5, migrate README. **Verified:** `cargo test -p lifeguard --lib query::table::definition`, `cargo test -p lifeguard-derive`, `cargo test -p lifeguard-migrate --lib`, `cargo test -p lifeguard-migrate --test test_index_key_list_sql_generation`, `cargo clippy -p lifeguard -p lifeguard-derive -p lifeguard-migrate --all-targets --all-features -- -D warnings`. **Follow-on:** CI `compare-schema`; richer WHERE/INCLUDE parse from `indexdef`; non-btree / GiST in infer. **Re-verified (this session):** `cargo test -p lifeguard --lib query::table::definition`, `cargo test -p lifeguard-migrate --lib schema_infer`, `cargo test -p lifeguard-derive --lib attributes` — all pass; structured IR + infer `#[index]` + PRD/SEAORM/DESIGN/README docs confirmed in tree.
- **T3 v2 + T2b follow-on + ordering/collation (2026-04-01):** [`fetch_live_btree_index_key_catalog_slots`] (`pg_get_indexdef` + `indcollation` + `indoption`); [`normalize_index_key_slot_for_compare`], [`parse_pg_indexdef_key_segment_strings`]; **T3 v2** compares normalized slots when either side has expression keys — match skips **T1**, mismatch → [`IndexKeyNormalizedSlotsMismatchDrift`]. **T2b:** expected opclass = merged explicit `_ops` token or catalog default; [`IndexBtreeNonDefaultOpclassDrift::migration_explicit_opclass`]. **T1** dedupe when only simple-key opclasses differ (tails after key list must match). **Ordering:** explicit migration `COLLATE` / `ASC|DESC` / `NULLS` vs live → [`IndexBtreeKeyOrderingCollationDrift`]. Smoke: `compare_no_opclass_drift_when_migration_explicit_matches_live`, `compare_t3_v2_skips_t1_when_expression_indexdefs_normalize_equal`, `compare_reports_ordering_drift_when_migration_desc_not_live_asc`. Docs/README/roadmap/DESIGN updates. Verified: `cargo test -p lifeguard-migrate`, `cargo clippy -p lifeguard-migrate --all-targets --all-features -- -D warnings`.
- **T3 partial — expression btree keys vs simple migration (2026-03-28):** [`fetch_live_btree_expression_index_key_slots`] + [`IndexExpressionKeyVsSimpleMigrationDrift`]; `compare_generated_dir_to_live_db` emits structured drift when merged `CREATE INDEX` parses as simple keys only and live `pg_index.indkey` has expression slot(s); **T1** suppressed for that index. Smoke: `fetch_live_btree_expression_index_key_slots_lists_lower_email`, `compare_reports_expression_key_when_migration_lists_simple_columns_only`. Docs: `lifeguard-migrate/README`, `DESIGN_INDEX_COMPARE_ROADMAP.md`, `DESIGN_INDEX_COMPARE_T2B_T3.md`, `ROADMAP.md`, `main.rs` compare-schema error hint. Verified: `cargo test -p lifeguard-migrate`, `cargo clippy -p lifeguard-migrate --all-targets --all-features -- -D warnings`.
- **Docs — DESIGN_FIND_RELATED_SCOPES appendix A (2026-03-28):** Aligned §A with shipped **T2b** (catalog btree non-default opclass drift); replaced mistaken **`jsonb_path_ops` as btree** example with **`text_pattern_ops` / `text_ops`** and noted **`jsonb_path_ops` is GIN-only**; listed remaining gaps (collation, NULLS order, expression slots, migration expected opclass). Verified: `cargo test -p lifeguard-migrate --test migration_db_compare_smoke` (7 pass).
- **T2b catalog query shipped (2026-03-28):** `fetch_live_btree_index_key_opclasses` — `pg_index`/`pg_opclass`/`opcdefault` for btree keys; `index_btree_nondefault_opclass_drifts` on shared tables in `compare_generated_dir_to_live_db`; smoke tests `fetch_live_btree_index_key_opclasses_lists_text_pattern_ops`, `compare_reports_btree_non_default_opclass_when_live_uses_text_pattern_ops` (btree `text_pattern_ops`; not `jsonb_path_ops` — GIN-only); README + `DESIGN_INDEX_COMPARE_*` + `ROADMAP.md`. Verified: `cargo test -p lifeguard-migrate`, `cargo clippy -p lifeguard-migrate --all-targets --all-features -- -D warnings`.
- **Design doc T2b / T3 (2026-03-28):** Added `docs/planning/DESIGN_INDEX_COMPARE_T2B_T3.md` — btree opclass parity (catalog vs `indexdef` parse, `IndexDefinition` gaps, drift shape, phases) and expression / functional keys (`pg_index` / `pg_get_expr`, taxonomy vs T1, optional derive extension). Linked from `DESIGN_INDEX_COMPARE_ROADMAP.md`, `docs/planning/README.md`, `lifeguard-migrate/README.md`.
- **compare-schema T1 + T4 + docs (2026-03-28 session):** `index_statements_for_table_from_merged_baseline` / `try_parse_create_index_statement` in `generated_migration_diff`; `normalize_index_statement_for_compare` (fix `using btree` prefix length), `IndexDefinitionTextDrift`, `IndexOnlyInDatabaseDrift`, `IndexOnlyInMigrationDrift`, dedupe vs column/access-method drifts in `compare_generated_dir_to_live_db`; `#[require_index_coverage]` + `validate_require_index_coverage` in `lifeguard-derive`; trybuild `compile_error_require_index_coverage`; `DESIGN_INDEX_COMPARE_ROADMAP.md`, `lifeguard-migrate/README.md` compare-schema section, `ROADMAP.md` row; CLI error text. Verified: `cargo test -p lifeguard-migrate`, `cargo test -p lifeguard-derive --test ui compile_error_require_index_coverage`, `cargo clippy -p lifeguard-migrate -p lifeguard-derive --all-targets --all-features -- -D warnings`.
- **PRD closure + compare-schema T2 (2026-03-28):** `IndexAccessMethodDrift` / `parse_pg_indexdef_access_method`; PRD §0.1 §5.7a §7.7 §9.7; `DESIGN_INDEX_COMPARE_ROADMAP`; `ROADMAP`; `SEAORM` snapshot; migrate README limits; `DESIGN_FIND_RELATED` appendix A. Verified: `cargo test -p lifeguard-migrate`, `cargo clippy -p lifeguard-migrate --all-targets --all-features -- -D warnings`.
- **PRD resume — spike + index roadmap (2026-03-28):** `DESIGN_INHERITED_PARENT_SCOPES_SPIKE.md` completed (recommendation A+D; B/C defer). New `DESIGN_INDEX_COMPARE_ROADMAP.md` (§5.7a T1–T4). PRD §0.3/§0.4, §5.7a, §7.7, §9.7; `PRD_FOLLOWON_NEXT_THREE` §3; `DESIGN_FIND_RELATED_SCOPES` appendix A/C; `lifeguard-migrate/README` updated.
- **PRD follow-on implemented (2026-03-28):** `DEVELOPMENT.md` G6 section; `examples/find_related_scope_example.rs` + `Cargo.toml` `[[example]]`; `DESIGN_INHERITED_PARENT_SCOPES_SPIKE.md`; `PRD_FOLLOWON_NEXT_THREE.md` / `PRD` §0.4 / `DESIGN_FIND_RELATED_SCOPES` / `README` / `docs/planning/README` updated. Verified: `cargo clippy -p lifeguard --all-targets --all-features -- -D warnings`.
- **Docs — PRD follow-on next three expanded (2026-03-28):** `docs/planning/PRD_FOLLOWON_NEXT_THREE.md` — G6/COMPARISON+SEAORM hygiene, `find_related`+scope example surface (`examples/` vs integration tests), inherited parent `SelectQuery`+loader merge (risks, spike directions). PRD §0.4 + DESIGN_FIND_RELATED_SCOPES cross-link.
- **Docs — deferred `compare-schema` / `FindRelated` semantics (2026-03-28):** Repository appendix in `docs/planning/DESIGN_FIND_RELATED_SCOPES.md` (opclass/index parity vs `pg_indexes`, `find_related_parent_scoped` + `has_many_through`, implicit parent-query merge); `lifeguard-migrate/README.md` **limits and roadmap** table + anchor; `lifeguard_migrate::schema_migration_compare` module + `MigrationDbCompareReport`/`IndexColumnDrift` rustdocs; `FindRelated` + `query::scope` user-facing rustdocs; PRD §5.7 Phase A closure + §7.7 pointer to DESIGN appendix §C. Verified: `cargo doc -p lifeguard -p lifeguard-migrate --no-deps` (exit 0).
- **PRD §5.7a / §7.7 / G6 batch (2026-03-28):** `IndexDefinition::include_columns`, `#[index = "... INCLUDE (...)"]` parse + `sql_generator` emits `INCLUDE`; `parse_pg_indexdef_include_columns` + `compare-schema` drift on key+INCLUDE names; **`FindRelated::find_related_parent_scoped`** + `join_tbl_on_expr` export; integration `test_find_related_parent_scoped_joins_from_table`. Docs: PRD, `DESIGN_FIND_RELATED_SCOPES.md`, `COMPARISON.md`, `SEAORM_LIFEGUARD_MAPPING.md`, migrate README. Verified: `cargo clippy -p lifeguard -p lifeguard-derive -p lifeguard-migrate --all-targets --all-features -- -D warnings`, `cargo test -p lifeguard-migrate`, `cargo test -p lifeguard --lib`, `cargo test -p lifeguard --test db_integration_suite related_trait::test_find_related_parent_scoped_joins_from_table`.
- **infer-schema golden bless workflow (2026-03-28):** `LIFEGUARD_BLESS_INFER_SCHEMA_GOLDENS=1` + `assert_infer_schema_golden` in `schema_infer.rs` tests; `just bless-infer-schema-goldens`; PRD §5.7a; `lifeguard-migrate/README.md`, `DEVELOPMENT.md`, `DEV_RUSTDOC_AND_COVERAGE.md`. Verified: `cargo test -p lifeguard-migrate`, `cargo clippy -p lifeguard-migrate --all-targets --all-features -- -D warnings`.
- **`compare-schema` index key reconciliation (2026-03-28):** `schema_migration_compare` — `pg_indexes` + `parse_pg_indexdef_simple_columns`; `IndexColumnDrift` / `MigrationDbCompareReport::index_column_drifts`; CLI error text mentions index keys. Integration: `migration_db_compare_smoke` — `compare_reports_column_and_index_drift_when_live_has_extra_indexed_column`, `compare_schema_cli_fails_when_live_has_extra_indexed_column`. Docs: `lifeguard-migrate/README.md`, PRD §5.7/§5.7a, `DEVELOPMENT.md`, `DESIGN_SCHEMA_INFERENCE_CLI_CODEGEN.md`. Verified: `cargo test -p lifeguard-migrate`, `cargo clippy -p lifeguard-migrate --all-targets --all-features -- -D warnings`.
- **`infer-schema --watch` (2026-03-31):** `lifeguard-migrate` polls DB, prints when `infer_schema_rust` output changes; `--watch-interval-secs` (default 5); CLI unit tests in `main.rs`; PRD §5.7/§5.7a + `lifeguard-migrate/README.md`. Verified: `cargo test -p lifeguard-migrate infer_schema_`, `cargo build -p lifeguard-migrate`.
- **`FindRelated::find_related_scoped` (2026-03-31):** Default trait method `find_related_scoped(related_scope)` = `find_related()?.scope(…)` (related-side only); `tests/db_integration/related_trait.rs` `test_find_related_scoped_matches_chained_scope`. DESIGN / PRD §0.3 row 4 / §7.7, SEAORM, `query::scope`, COMPARISON. Verified: `cargo test -p lifeguard --test db_integration_suite test_find_related_scoped`, `cargo clippy -p lifeguard --all-targets --all-features -- -D warnings`.
- **`#[scope_bundle]` (2026-03-31):** `lifeguard_derive::scope_bundle` + `lifeguard::scope_bundle` — AND-combines `#[scope]` names into `pub fn scope_<name>() -> sea_query::Condition`; tests `scope_bundle_and_chains` in `lifeguard-derive/tests/test_minimal.rs`. PRD §0.3 row 3 / §7.7, `query::scope` rustdoc, `SEAORM_LIFEGUARD_MAPPING.md`. Verified: `cargo test -p lifeguard-derive`, `cargo clippy -p lifeguard-derive -p lifeguard --all-targets --all-features -- -D warnings`.
- **PRD follow-on §7.7 / §0.3 (2026-03-31):** Prioritized Phase C vs E in PRD **§0.3**; integration test `test_find_related_chains_scope_on_related_query` in `tests/db_integration/related_trait.rs`; `DESIGN_FIND_RELATED_SCOPES.md`, `SEAORM_LIFEGUARD_MAPPING.md`, PRD §7.7 / §9.7 updated. Verified: `cargo test -p lifeguard --test db_integration_suite test_find_related_chains_scope_on_related_query`.
- **lifeguard-derive `validate_fields` + Aggregate (2026-03-31):** `#[validate(custom)]` FailFast vs Aggregate branches in `life_record.rs`; struct-level `#[validation_strategy = "aggregate"|"fail_fast"]` parsed in `attributes.rs`, registered on `LifeModel`/`LifeRecord` derives, emits `ActiveModelBehavior::validation_strategy` when set (so `validate_fields` matches `run_validators` — manual second `impl ActiveModelBehavior` is invalid). Tests `validate_multi_fail_fast` / `validate_multi_aggregate` in `lifeguard-derive/tests/test_minimal.rs`; shared `name_non_empty` in `validate_attr_tests`. Verified: `cargo test -p lifeguard-derive`, `cargo clippy -p lifeguard-derive --all-targets --all-features -- -D warnings`.
- **README reorg (2026-03-28):** Landing page split — **`ROADMAP.md`**, **`COMPARISON.md`** (repository truth under **`#repository-status`** + competitive matrix + ecosystem + performance). **`STATUS.md`** merged into **`COMPARISON.md`** (2026-03-28). **`README.md`** is pitch + getting started + doc index table. **`ARCHITECTURE.md`** LifeReflector link points to **`VISION.md#the-killer-feature-lifereflector`**. PRD + SEAORM mapping reference **`COMPARISON.md`**.
- **PRD follow-on:** Phase E **insert-only** session flush for new rows (no PK); `find_related` + scopes per `docs/planning/DESIGN_FIND_RELATED_SCOPES.md`; optional `infer-schema` CLI / CI smoke.
- **Dev process + Phase D v0 (2026-03-28):** `docs/planning/DEV_RUSTDOC_AND_COVERAGE.md` (rustdoc + coverage checklist); Phase D `ColumnTrait::f_*` for SeaQuery `UPDATE SET`.
- **PRD Phase C v0 — scopes (2026-03-28):** `SelectQuery::scope`, `IntoScope`, `src/query/scope.rs` docs + tests; PRD §7.7; mapping doc row. Next: Phase D F() or README G6 follow-ups if requested.
- **PRD Phase B v0 — validators (2026-03-28):** Landed trait-based validation pipeline + `ActiveModelError::Validation`; derive wires `run_validators` after `before_insert`/`before_update`. PRD `docs/planning/PRD_SCHEMA_VALIDATORS_SESSION_AND_SCOPES.md` §6.7 + milestone §0.1. Memory bank updated with this line.
- **Pool observability docs + metrics cardinality (2026-03-28):** README pool status/section updated (shipped features, `pool_tier`, links to POOLING_OPERATIONS / DESIGN / OBSERVABILITY). Prometheus/OpenTelemetry: new gauge `lifeguard_pool_workers` + `pool_tier` on pool counters/histograms; `METRICS` API extended (`set_pool_workers_by_tier`, optional `pool_tier` on query/wait helpers, tier on pool-only counters). Rustdoc: `lib.rs`, `pool/mod.rs`, `pooled.rs` module docs. `docs/OBSERVABILITY.md` metric table has label column. `CHANGELOG` Unreleased. Verified `cargo test -p lifeguard --lib --features metrics,tracing` (395 pass). `db_integration_suite` hit SIGSEGV in this environment (likely infra); not attributed to this change.
- **Tilt observability (2026-03-30):** `config/k8s/observability/` — kustomize applies OTEL Collector, Prometheus, Loki, Grafana in `lifeguard-test` after test-infrastructure; `Tiltfile` wires `k8s_resource` port-forwards: Grafana **3000**, Prometheus **9090**, Loki **3100**, OTEL **4317/4318/9464**. Loki config includes `compactor.working_directory` under `/data` (fixes CrashLoop). OTEL + Grafana datasource YAMLs: canonical paths `config/k8s/observability/grafana-datasources.yml` and `otel-collector-config.yml`; `config/grafana-datasources.yml` and `config/otel-collector-config.yml` symlink to them. `scripts/dev_up.py` logs the same host ports. **Grafana:** provisioned folder **Lifeguard**, dashboard UID **`lifeguard-kind-infra`** (`grafana-dashboard-lifeguard-kind.json`); **postgres-exporter** (`DATA_SOURCE_NAME` DSN to primary) + **redis-exporter**; Prometheus scrape jobs for both; panels use **`pg_up`**, **`pg_stat_replication_pg_wal_lsn_diff`** (per replica IP), Redis memory/clients/commands. **Note:** this Cursor agent session does not attach the user’s **Grafana MCP** (`call_mcp_tool` lists no `grafana` server); use Grafana MCP from a Cursor client where it is enabled, or iterate on the JSON file.
- **Kind Postgres validation (2026-03-30):** Fresh `kind-lifeguard-test` + Tilt — `kubectl` all deployments Ready; `pg_stat_replication` shows 2× `streaming` async standbys; Redis PONG; `cargo nextest` `db_integration_suite` **65/65** pass; workspace **818** pass **2** skip (`lifeguard-integration-tests` migration tests). Replica-1 smoke (`TEST_REPLICA_URL` :6546) passes for `pooled_pool_construct_write_read_with_replica`. Documented checklist + coverage gaps (Toxiproxy fallback no-op on Kind, second replica optional) in `docs/TEST_INFRASTRUCTURE.md` § Kind Postgres & Redis validation.
- **JSF-style safety audit — PRD P1–P4 landed in tree (2026-03-28):** Library + derive hygiene per `docs/planning/audits/PRD_JSF_PANIC_SAFETY.md`; remaining long-tail items (if any) are policy/architecture (e.g. metrics init) or out-of-scope binaries, not open Phase 4 tasks.
- ModelTrait edge case coverage implementation completed
- Comprehensive test suite added (Option<T> and JSON types)
- Documentation improvements for edge cases and limitations
- Memory Bank initialized with codegen learnings

## Recent Changes
- **Transcript audit (2026-04-03):** Searched `~/.cursor/projects/.../lifeguard/agent-transcripts` for wording like “commit what we have in lifeguard” plus “short break” / “recover.” **No saved user message** matches that full intent. Closest related threads: [Lifeguard commit session](27e9697a-b23d-43d9-8a30-6e92344d7e48) — user asked to commit Lifeguard after hauliage DB fixes (`c175ac3` / `b02c7fb` era) and later “commit lifeguard and push” (`b06df89`); “recover” hits are technical (mutex poison, CI/session notes), not a personal break.
- **Tilt `test-nextest` — `pg_index` slot ordinals (this session):** `generate_subscripts(indkey::int2[], 1)` can yield **0** when the array lower bound is **0**; `pg_get_indexdef(oid, 0, …)` returns the **full** `CREATE INDEX`, breaking T3 v2, expression-key fetch, and T2b. Fixed by **`unnest(indkey::int2[]) WITH ORDINALITY`** (1-based `key_ord` for `pg_get_indexdef`). **Follow-on:** multi-array `unnest(a,b,c) WITH ORDINALITY AS u(…)` under `CROSS JOIN LATERAL` hit **`syntax error at or near "AS"`** on some PostgreSQL builds (Kind/nextest); replaced with **single unnest +** `(parallel::type[])[array_lower(indkey::int2[],1) + key_ord - 1]` for `indcollation` / `indoption` / `indclass` in `schema_migration_compare.rs` and `schema_infer.rs`. Verified: `cargo test -p lifeguard-migrate --test migration_db_compare_smoke`, `cargo test -p lifeguard-migrate --lib schema_infer`, `cargo clippy -p lifeguard-migrate --all-targets --all-features -- -D warnings`.
- **Structured `IndexKeyPart` + infer `#[index]` closure (this session):** Memory bank re-verified after handoff; tests above pass; no further code changes required for the structured IR / infer-schema / docs follow-on batch.
- **Docs — COMPARISON ecosystem + PRD ValidateOp + progress Metrics (2026-03-28):** `COMPARISON.md` — factual **BRRTRouter** / **Lifeguard** / **`may`** vs **Tokio** / **`async`/`await`** / **SeaORM** / **SQLx** / **Diesel (sync)** / **Diesel-async** table; removed absolute “must”/“fundamentally incompatible” wording; performance footnote for Diesel. `PRD_SCHEMA_VALIDATORS_SESSION_AND_SCOPES.md` — `ValidateOp` = `Insert` \| `Update` \| `Delete`; V-1 insert/update/**delete**; §5.7 index scope + §5.7a **index reconciliation** stretch; `.agent/memory-bank/progress.md` **Metrics** block labeled **historical snapshot** with pointers to **Completed**.
- **`ActiveModelTrait::take` session notify (2026-03-28):** `lifeguard-derive` `life_record.rs` — `__lg_session_notify_dirty()` only when `take` actually changes state (`#field_name.is_some()` or `__update_exprs` contained that column), matching `set`/`set_col` gating. Tests `test_take_noop_when_unset_skips_session_dirty`, `test_take_with_value_or_expr_notifies_session_dirty` in `lifeguard-derive/tests/test_minimal.rs`. `cargo test -p lifeguard-derive`, `cargo clippy -p lifeguard-derive --all-targets --all-features -- -D warnings`.
- **`SessionIdentityModelCell` / `unsafe impl Send` (2026-03-28):** Cursor review — `Rc` + `unsafe Send` is unsound if the “one thread per `Rc` clone” protocol is violated; acknowledged. Expanded `src/session/identity_model_cell.rs` module docs + `// SAFETY` block: why `ActiveModelTrait: Send` forces `Option<SessionIdentityModelCell>: Send`; that `Arc<RefCell<M>>` does not restore `Send` (`RefCell: !Sync`); pointer to `SECURITY_PROMPT.md` §A.3 and `Arc<Mutex<M>>` as a type-sound long-term direction. No API change in this step.
- **`migration_db_compare_smoke` CLI assertion (2026-03-28):** `compare_schema_cli_succeeds_when_no_drift` asserted `Tables match` / `Success`, but `MigrationDbCompareReport`’s `Display` prints **`No drift`** when `!has_drift()` (`schema_migration_compare.rs`). Test now asserts `stdout.contains("No drift")`; stderr assert uses named `stderr=` placeholder.
- **Review nits (2026-03-28):** `tests/db_integration/pool_read_replica.rs` — per-test unique `pool_replica_test_{n}` / `t_pool_replica_smoke_{n}` + `setup_schema_on_primary(executor, schema, table)` to avoid parallel DDL collisions; raw `may_postgres` `query_one` uses a local `String` + `as_str()`. `SessionIdentityModelCell::replace_with` → `try_borrow_mut` + `Result<(), BorrowMutError>`; derive `__lg_session_notify_dirty` documents `to_model()` clone cost + ignores replace error. `DEVELOPMENT.md` — split `lifeguard-migrate` / optional live Postgres files / `db_integration_suite` bullets. `lifeguard-migrate` / tests — named `format!` where applicable; `src/session/uow.rs` left unchanged (already `{e}`/`{rb_err}`; explicit `err = e` trips `clippy::uninlined_format_args`). `predicates::string_utf8_chars_max` already single `chars().count()`. `ModelIdentityMap::register_pending_insert` rustdoc: `PENDING_INSERT_KEY_PREFIX`, `next_pending_id`, wrapping. Verified: `cargo clippy -p lifeguard -p lifeguard-derive -p lifeguard-migrate --all-targets --all-features -- -D warnings`, `cargo test -p lifeguard --lib session::`, `cargo test -p lifeguard-derive`.
- **ActiveModelTrait `set` / `set_col` vs `__update_exprs` (2026-03-28):** `lifeguard-derive` `life_record.rs` — match arms remove a pending `set_*_expr` only after `Value`→field conversion succeeds, so `InvalidValueType` no longer drops the scheduled F-style expression. Integration tests in `lifeguard-derive/tests/test_minimal.rs` (`test_set_invalid_type_preserves_update_expr`, `test_set_col_invalid_type_preserves_update_expr`, non-PK column). Verified: `cargo test -p lifeguard-derive`; `cargo clippy -p lifeguard -p lifeguard-derive --all-targets --all-features -- -D warnings`.
- **Toxiproxy fallback test ignored (2026-03-28):** `pooled_read_falls_back_to_primary_when_replica_lagging` is `#[ignore]` (flaky CI: `is_replica_lagging` after proxy disable). Run with `cargo test -- --ignored` when debugging; TODO to stabilize `WalLagMonitor` + re-enable.
- **Tiltfile — replication test resources (2026-03-28):** Targeted nextest runs for read-replica PRD work (`test-replication-pool`, `test-db-integration-replica`, `test-replication-pool-smoke`) with Kind ports 6543/6544/6545. **`test-replication-pool` / `-smoke`:** added nextest `--status-level pass` so filtered runs do not print one SKIP line per non-matching test in `db_integration_suite` (filter is intentionally narrow; full replica env suite remains `test-db-integration-replica`).
- **CI Compose + Toxiproxy (2026-03-30):** [Toxiproxy](https://github.com/Shopify/toxiproxy) fronts the streaming replica on host **6547** (API **8474**) so Kind/Tilt can keep **6544** for replica-0. The integration test that PATCHes proxy `enabled` for primary fallback is **`#[ignore]`** until stabilized (see Recent Changes). **Local:** stop Tilt or avoid port collisions — `127.0.0.1:6543` may hit Kind vs Compose primary if both bind.
- **Tiltfile — all workspace crates + deps (2026-03-28):** Build resources for `lifeguard-codegen`, `lifeguard-reflector`, `lifeguard-migrate` with `resource_deps` matching Cargo (`migrate` after `build-lifeguard`; derive/codegen/reflector parallel). Tests wired to those builds where appropriate.
- **Read-replica CI/testing documentation (2026-03-28):** PRD + design doc under `docs/planning/`; `TEST_INFRASTRUCTURE.md` links. Implementation (Compose, harness, tests) deferred.
- **`LifeguardPool::new` four-arg API + transparent routing (2026-03-28):** Single pool type holds primary workers and optional replica workers; `PooledLifeExecutor` dispatches writes to primary and reads to replica when lag allows. Call sites: primary-only is `replica_urls: vec![], replica_pool_size: 0`. `perf_orm` updated accordingly.
- **Unsigned row decode (2026-03-28):** `lifeguard::from_row_unsigned_try_from_failed` + derive emits `TryFrom` for `u8`/`u16`/`u32`/`u64` in `from_row` / `life_model` / `partial_model`; `FromRow` standalone uses `try_get`. Landed on branch `fix/JSF-compliance-issues` (committed locally, not pushed).
- **lifeguard-derive Clippy (2026-03-28):** Fixed `explicit_iter_loop` in `from_row.rs` and `manual_let_else` in `relation.rs` / `partial_model.rs` so `cargo clippy -p lifeguard-derive --all-targets --all-features -- -D warnings` passes (pedantic-style lints from full-workspace clippy).
- **PRD JSF panic safety — implemented P1–P4 (2026-03-28):** P1–P3 as before. **P4:** `lifeguard-derive` `#![deny(clippy::unwrap_used)]`; `utils::field_ident` in `life_record` / `life_model` / `from_row` / `try_into_model` / `partial_model`; generated `graph_mut` uses `get_or_insert_with`; `Relation::def()` → `Option<RelationDef>` + tests/PRD §12; scoped `unwrap_used` allows in derive test modules only. Verified: `cargo clippy -p lifeguard-derive --all-targets --all-features -- -D warnings`, `cargo test -p lifeguard-derive`.
- **JSF / unwrap-expect audit vs BRRTRouter docs (2026-03-28):** Written summary for production hardening (library vs derive vs generated user code vs binaries); no code changes in this step.
- **`#[cursor_tiebreak]` opt-in (2026-03-28):** `LifeModel` no longer injects `#[cursor_tiebreak = …]` for every single-PK entity; only forwards when the user sets `#[cursor_tiebreak = "ColumnVariant"]` on the struct (`extract_cursor_tiebreak`). Composite PK + attribute → compile error. `LifeModel` / `LifeRecord` derive attribute lists updated. Integration `DataPoint` uses `#[cursor_tiebreak = "Id"]` for `after_pk` test. Rustdoc in `query/traits.rs` and `query/cursor.rs` updated. `cargo test -p lifeguard-derive`, `db_integration_suite::test_pagination_and_streaming`, clippy `-D warnings` passed.
- **`lifeguard-derive` `entity.rs` `derive_entity` line count (2026-03-28):** Extracted `generate_life_model_trait_impl` plus `resolve_entity_model_name` / `resolve_entity_column_enum_name` / `default_stripped_suffix_ident` so `derive_entity` is under `clippy::too_many_lines` without crate-level allow; token fragments passed by reference (`needless_pass_by_value`). `cargo clippy -p lifeguard-derive -- -D warnings -W clippy::pedantic` and `cargo test -p lifeguard-derive` passed.
- **`find_linked` empty `Linked::via()` (2026-03-28):** Empty path no longer returns `Ok(SelectQuery::new())` (unrestricted target table); returns `LifeError::Other("find_linked: Linked::via() returned an empty path")`. Rustdoc updated; `test_find_linked_empty_path` asserts Err. `cargo test -p lifeguard --lib relation::traits::tests::test_find_linked_empty_path` and clippy `-D warnings` passed.
- **`stream_all` commit errors (2026-03-28):** After a successful fetch loop, `txn.commit()` errors are no longer discarded: send `LifeError::Other` with cursor name on the stream channel when the receiver is still open; if the receiver dropped, `log::warn!` with the same context. `cargo clippy -p lifeguard -- -D warnings` passed.
- **`SelectQuery::stream_all` + soft delete (2026-03-28):** `stream_all` used `self.query.build` and skipped `apply_soft_delete()` used by `all()` / `one()`. Fixed: `self.apply_soft_delete().build(PostgresQueryBuilder)` (consumes `self` like `all()`; no `&mut self`). `LifeModelTrait::soft_delete_column()` was never generated for `#[soft_delete]` entities, so `apply_soft_delete` was a no-op for `SelectQuery::new()`—added `soft_delete_column` → `Some(Column::DeletedAt)` in `DeriveEntity`. Integration test `test_stream_all_applies_soft_delete_like_all` (bare `SelectQuery::new()` vs soft-deleted row). `cargo test -p lifeguard-derive`, `cargo test -p lifeguard --test db_integration_suite …`, full `cargo clippy -- -D warnings` passed.
- **`lifeguard-derive` `#[belongs_to]` on ignored fields (2026-03-28):** Replaced `syn::parse_str(...).expect` and missing-`from` `.expect` with `parse_relation_entity_path` + `syn::Error::new_spanned` (same pattern as `#[has_many]` / `#[has_one]`). Unit test `invalid_belongs_to_entity_path_returns_syn_error_with_message`. `cargo test -p lifeguard-derive` passed.
- **`query/stream.rs` streaming txn cleanup (2026-03-28):** Code review: coroutine could panic after `begin()` and never `commit()`, leaving the cloned `may_postgres::Client` in an open transaction. Added `StreamingTxnGuard` (`Option<Transaction>` + `Drop` → `rollback()` if still held). Success path `take()` + `commit()`; `Err` from `with_converted_params` / fetch / parse → `take()` + `rollback()` (PostgreSQL aborted-txn semantics; replaces always-`commit` after error). `cargo clippy -p lifeguard -- -D warnings` and `cargo test -p lifeguard --lib` passed.
- **CI Clippy `pedantic` + `-D warnings` (2026-03-28):** Addressed failures from `cargo clippy --all-targets --all-features -- -D warnings -W clippy::pedantic` across `lifeguard` and `lifeguard-derive`: `expect_used` in cursor pagination and test modules (scoped allows / match instead of expect), `match_like_matches_macro` in `query/loader`, `uninlined_format_args` / `double_must_use` in `relation/def/condition`, derive macro `doc_markdown` / `too_many_lines` / `if_let_some_result` style fixes. Verified locally: **exit 0** on full command above.
- **README LifeReflector (2026-03-28):** Restored **How it works** as a numbered prose block (steps 1–7): read path populates Redis first; NOTIFY steps 2–6 refresh-or-ignore; step 7 cross-service reads. Fixes “ignore ⇒ Redis never fills” without dropping the original list style.
- **README.md (2026-03-28):** Aligned with repo reality — current status (executor/query/derive/migrations/logging), `LifeguardPool` not exported, removed duplicate intro, fixed derive example attrs, replaced fictitious `testkit`/pool getting-started with `test_helpers` + examples, honest roadmap table, documentation links (DEVELOPMENT, TEST_INFRASTRUCTURE, OBSERVABILITY, SEAORM mapping), dual-license note vs single `LICENSE` file.
- **Pre-commit clippy (2026-03-28):** `.pre-commit-config.yaml` — `always_run: true` on `cargo-clippy` so the hook is not skipped as `(no files to check)` when commits omit Rust paths. `DEVELOPMENT.md` updated (removed incorrect fmt+clippy claims). Verified `pre-commit run cargo-clippy` executes clippy (exit 101 on current tree when clippy fails).
- **GHA CI Postgres password (2026-03-28):** `.github/workflows/ci.yaml` uses repository secret `PGPASSWORD` for `POSTGRES_PASSWORD`, `DATABASE_URL` / `TEST_DATABASE_URL`, and job `PGPASSWORD` (psql). Removed hardcoded `export PGPASSWORD=postgres`. `docs/TEST_INFRASTRUCTURE.md` documents the secret and URL-reserved-character caveat.
- **`logging` `LogRecord::format_line` (2026-03-28):** `sanitize_single_line_field` escapes `\r`/`\n` in `message` and `active_span` so output stays one physical line (log-forging mitigation). Tests `format_line_escapes_newlines_in_message`, `format_line_escapes_cr_lf_in_message_and_span`. `cargo test -p lifeguard --lib logging` passed.
- **`logging` `ChannelLogger::flush` / `flush_log_channel` (2026-03-28):** Global may `mpsc` now carries `LogMsg::{Record,Flush}`; drain acknowledges `Flush` via `std::sync::mpsc` so `log::Log::flush` blocks until prior records are written. Reentrancy documented. `try_enqueue` maps `SendError<LogMsg>` back to `SendError<LogRecord>`. Test `log_msg_flush_waits_until_prior_records_processed`. `cargo test -p lifeguard --lib logging` passed.
- **CI / `generate-migrations` + `apply_order.txt` (2026-03-28):** `examples/entities` `generate_migrations` binary calls `write_apply_order_file` after writing SQL under `migrations/generated`. GHA applies `migrations/generated/apply_order.txt` (non-comment lines = paths relative to that dir), fallback `find|sort` if missing/empty. `docs/TEST_INFRASTRUCTURE.md` updated. Verified `cargo run --bin generate-migrations` from `examples/entities`.
- **`lifeguard-migrate` `extract_alter_table_targets_from_migration_sql` (2026-03-28):** Regex now skips PostgreSQL `IF EXISTS` and `ONLY` before the table name so `ALTER TABLE IF EXISTS foo ...` captures `foo`, not `IF`. Tests: `extract_alter_table_if_exists_table_name_not_if`, `extract_alter_table_only_before_name`, `extract_alter_table_if_exists_only_combined`. `cargo test -p lifeguard-migrate sql_dependency_order` passed.
- **`build_where_condition` missing FK (2026-03-28):** Follow-up to CodeRabbit: `FindRelated` / `FindLinked` rustdoc **Returns** now describe `Result<SelectQuery<_>, LifeError>` and `LifeError::Other` when relation keys are missing. Added unit test `test_build_where_condition_err_when_from_col_missing` in `src/relation/def/condition.rs`. `docs/planning/lifeguard-derive/AUTHORING_MODEL_TRAIT.md` updated (panic → `LifeError`). Verified `cargo test -p lifeguard --lib relation::`.
- **`query/loader.rs` NULL FK filtering (2026-03-28):** CodeRabbit cited an old `parent_ids` loop that only skipped `String`/`Int` None; current code already uses `relation_side_value_present` + `extract_fk_tuple` covering primitives, `Json`, and chrono `None` for enabled `sea-query` features. Added doc note + unit test `relation_side_value_present_filters_null_variants`; `cargo test -p lifeguard query::loader` passed.
- **`lifeguard-derive` `life_model.rs` (2026-03-28):** Macro-generated `RelationDef` for `#[has_many]` / `#[belongs_to]` / `#[has_one]` used `ConditionType::Any` (OR) on `From<RelationDef> for Condition` join building; switched to `ConditionType::All` to match hand-written defs and composite-key AND semantics. `cargo test -p lifeguard-derive` passed.
- **`tests/context.rs` Postgres URL (2026-03-28):** `postgres_url_from_env()` now reads only `TEST_DATABASE_URL` (no `DATABASE_URL` fallback) so destructive integration helpers cannot target a shell-level app DB. `just nt-db-suite` sets `TEST_DATABASE_URL` from the recipe’s `DATABASE_URL` default; CI already exports both. Docs (`TEST_INFRASTRUCTURE.md`), `nextest.toml` comments, and workflow comment updated.
- **`build_where_condition` / LazyLoader / find_related (2026-03-28):** Replaced `Expr::cust("{table}.{col}")` with `Expr::col(ColumnName(Some(to_tbl), col))` or `(alias, col)` so schema-qualified `RelationDef::to_tbl` and reserved identifiers match `SelectQuery` / Postgres quoting. Added `test_build_where_condition_schema_qualified_to_tbl`. Updated `RELATION_WHERE_EXPR_DECISION.md`. `join_tbl_on_*` still uses `cust` (documented follow-up).
- **`src/query/traits.rs` doctests (2026-03-28):** CodeRabbit was correct: passing `executor: &dyn LifeExecutor` into generic `&E where E: LifeExecutor` infers `E = dyn LifeExecutor`, which fails the implicit `Sized` bound. Fix: pass **`&executor`** so `E` is `&dyn LifeExecutor` (blanket `impl LifeExecutor for &dyn LifeExecutor`). Module docs now explain this; examples use `&executor`, `Result`-returning `main`, and minimal `UserColumn` / `ModelTrait` / `ActiveModelTrait` stubs so `cargo test -p lifeguard --doc query::traits` passes (6 tests).
- **`RelationLoader` composite keys (2026-03-28):** `Identity::Unary`/`Binary`/… column names via `identity.iter()`; unique parent FK tuples; unary uses `is_in`, multi-column uses `OR` of `AND` equality on `to_col` names; grouping/inject uses `fk_tuple_map_key` over all parts. Child fetch uses `with_converted_params` + `apply_soft_delete` (not `SelectQuery::all`) so `dyn LifeExecutor` works. Unit test `fk_tuple_map_key_distinct`.
- **`RelationLoader` (2026-03-28):** Replaced `grouped_children.remove` with `get` + `Vec::clone` so every parent with the same FK (BelongsTo) receives related rows; `R::Model: Clone` on `RelationLoader` and `SelectQuery::load`. Integration test `test_dataloader_belongs_to_duplicate_parent_fk`.
- **`CursorPaginator` / cursor tie-break (2026-03-28):** Optional stable ordering via `(cursor column, single PK)` using `after`+`after_pk` / `before`+`before_pk` when `LifeModelTrait::cursor_tiebreak_column()` is set (**opt-in** `#[cursor_tiebreak = "PkVariant"]` on the `LifeModel` struct, single-column PK). `ORDER BY` uses both columns; `WHERE` uses lexicographic keyset predicates. Without the attribute, tie-break stays `None`. Integration test covers duplicate `val` with distinct ids; streaming expectations updated to 13 rows.
- **`lifeguard-migrate` `order_migrations_by_foreign_key_sql` (2026-03-28):** Filter out self-referential `REFERENCES` targets (`d != name`) so single-table self-FKs do not create a false cycle in `topological_sort`. Test: `order_migrations_self_referential_fk_single_table`.
- **`lifeguard-migrate` `sql_dependency_order.rs` (2026-03-28):** `extract_created_tables_from_migration_sql` returns all `CREATE TABLE IF NOT EXISTS` names per file (captures_iter + normalize + sort/dedup); `FileMeta.created` is `Vec<String>`; `table_creator` registers every table; `ALTER TABLE` prerequisite edges no longer gated on “no CREATE in file” so mixed create+alter migrations get correct ordering. Added unit tests including `apply_order_registers_every_create_in_file`. `cargo test -p lifeguard-migrate` passed.
- **lifeguard-derive `life_model.rs` (2026-03-28):** Gated collection of field `#[graphql(...)]` pass-through with `cfg(feature = "graphql")` so generated Model fields do not carry orphaned graphql attrs when `SimpleObject` is not emitted (matches existing `graphql_derive` gate). Verified with `cargo check -p lifeguard` (no graphql), `cargo check -p lifeguard --features graphql`, and `cargo test -p lifeguard-derive`.
- **CI `.github/workflows/ci.yaml` (2026-03-28):** `test` / `perf_orm` use **only** `.github/docker/docker-compose.yml` for Postgres primary + replica + Redis (no `jobs.*.services` for those). Host ports **6543 / 6544 / 6545** (Compose) so **5432/5433/6379** stay free for Kind/Tilt; job `env` sets `REDIS_URL` / `TEST_REDIS_URL` → `127.0.0.1:6545` when using that stack.
- **lifeguard-derive `life_model.rs` (2026-03-28):** Replaced `.expect()` in ignored-field `#[has_many]` / `#[belongs_to]` / `#[has_one]` handling with `syn::Error` + `to_compile_error()`; added `relation_attr_on_field` + `parse_relation_entity_path` helpers and unit test `invalid_entity_path_returns_syn_error_with_message`. Full `cargo test -p lifeguard-derive` passed.
- Added 14 new tests for Option<T> and JSON field handling
- Improved documentation for missing primary keys and unsupported types
- Added warnings in generated code for edge cases
- Updated EDGE_CASES_ANALYSIS.md with current status
- Initialized Memory Bank with project context

## Key Learnings (Codegen & Derive Setup)
1. **Nested Macro Expansion**: LifeModel generates Entity with `#[derive(DeriveEntity)]` for nested expansion
2. **Column Enum Requirements**: Must implement both `Iden` and `IdenStatic` traits
3. **Type Extraction**: Use helper functions like `extract_option_inner_type()` for Option<T>
4. **Match Arm Generation**: Build vectors of match arms, then quote! them together
5. **Name Conflict Resolution**: Use separate modules in tests to avoid conflicts
6. **E0223 Error Fix**: Use direct type references (`Column`) instead of `Entity::Column`
7. **JSON as Core Feature**: No feature flags, always enabled via direct dependencies
8. **Primary Key Tracking**: Track first `#[primary_key]` field, generate warning if none found

## Branch
- `fix/JSF-compliance-issues` — committed `0d04c00` (JSF PRD P1–P3 + implementation); pushed to `origin/fix/JSF-compliance-issues` (`git push -u` — `farm git push` had no upstream). No Cursor co-author trailer.
- `fix/tests` — pushed `e5cf177` (relations/logging/migrations/CI); no Cursor co-author trailer.

## Next Steps
- Create PR using farm tools
- Continue with next core traits and types from SEAORM_LIFEGUARD_MAPPING.md
