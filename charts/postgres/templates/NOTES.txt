Lifeguard postgres (primary-direct, no Pgpool)

  Primary Deployment: {{ include "postgres.primaryName" . }}
  App Service:        {{ include "postgres.serviceName" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}
  Secret:             {{ include "postgres.secretName" . }}

Connect (in-cluster):
  psql -h {{ include "postgres.serviceName" . }}.{{ .Release.Namespace }}.svc.cluster.local -U {{ .Values.auth.username }} -d {{ .Values.auth.database }}

{{- if and .Values.replica.enabled (gt (int .Values.replica.count) 0) }}
Streaming replicas: {{ .Values.replica.count }} ({{ .Values.replica.namePrefix }}-0..)
  These are NOT selected by the app Service — use for Lifeguard WAL-lag tests only.
{{- else }}
Streaming replicas: disabled (recommended for shared Kind).
{{- end }}

Shared Kind cutover tip: use values/shared-kind.yaml so Service name stays `postgres`
and auth keys match data/postgres-credentials.
