apiVersion: v2
name: postgres
description: >
  Lifeguard-oriented PostgreSQL for Kind/Tilt/shared-dev: Bitnami primary
  Deployment with optional streaming standbys. App traffic hits the primary
  Service directly (no Pgpool). Matches .github/docker/docker-compose.yml and
  config/k8s/test-infrastructure.
type: application
version: 0.1.0
appVersion: "15"
keywords:
  - postgresql
  - lifeguard
  - kind
maintainers:
  - name: microscaler
sources:
  - https://github.com/microscaler/lifeguard
