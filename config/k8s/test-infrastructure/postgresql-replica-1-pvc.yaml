apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgresql-replica-1-data
  namespace: lifeguard-test
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
