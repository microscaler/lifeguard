apiVersion: v1
kind: Service
metadata:
  name: postgresql-primary
  namespace: lifeguard-test
  labels:
    app: postgresql-primary
spec:
  type: ClusterIP
  ports:
    - name: postgresql
      port: 5432
      targetPort: postgresql
      protocol: TCP
  selector:
    app: postgresql-primary
